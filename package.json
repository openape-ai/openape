{
  "name": "openape",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@10.29.3",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "turbo run build",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "test": "turbo run test",
    "dev": "turbo run dev",
    "ci:app:openape-agent-mail": "pnpm --filter ./apps/openape-agent-mail run lint && pnpm --filter ./apps/openape-agent-mail run typecheck && pnpm --filter ./apps/openape-agent-mail run test:run && pnpm --filter ./apps/openape-agent-mail run build",
    "ci:app:openape-agent-proxy": "pnpm --filter ./apps/openape-agent-proxy run lint && pnpm --filter ./apps/openape-agent-proxy run typecheck && pnpm --filter ./apps/openape-agent-proxy run build",
    "ci:app:service": "pnpm --filter ./apps/service run lint && pnpm --filter ./apps/service run typecheck && pnpm --filter ./apps/service run test:run && pnpm --filter ./apps/service run build",
    "changeset": "changeset",
    "version-packages": "changeset version",
    "release": "node scripts/publish-chain.mjs",
    "release:dry": "node scripts/publish-chain.mjs --dry-run"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "@parcel/watcher",
      "better-sqlite3",
      "esbuild",
      "sharp",
      "unrs-resolver",
      "vue-demi"
    ],
    "overrides": {
      "eslint": "^9.35.0"
    }
  },
  "devDependencies": {
    "@antfu/eslint-config": "catalog:",
    "@changesets/changelog-github": "^0.5.0",
    "@changesets/cli": "catalog:",
    "eslint": "catalog:",
    "turbo": "^2.5.0"
  }
}
