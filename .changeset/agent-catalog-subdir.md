---
"@openape/apes": minor
---

Recipe refs support catalog subdirectories: `<owner>/<repo>/<subdir>@<ref>` clones the repo at the pinned ref and uses only the subdirectory as the recipe root (e.g. `openape-ai/agent-catalog/ceo@v0.1.0`).
