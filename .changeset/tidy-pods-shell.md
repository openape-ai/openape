---
"@openape/apes": patch
---

Support an explicit Pod target host, clean interactive bash startup, and an opt-in socket descriptor for supervised script execution through ape-shell. Preserve the ordinary CLI defaults.
