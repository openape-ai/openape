---
"@openape/apes": patch
---

Support an explicit Pod target host, clean interactive bash startup, and an opt-in socket descriptor for supervised script execution through ape-shell. Add explicit suppression of writable HOME configuration for managed runtimes. Preserve the ordinary CLI defaults.
