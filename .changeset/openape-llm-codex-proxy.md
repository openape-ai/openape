---
---

openape-llm container now runs @openape/codex-proxy instead of the litellm fork (adds gpt-5.5 over the ChatGPT subscription). App/config + a tooling-only `.dockerignore` for @openape/codex-proxy — no package release.
