{
  "name": "openape-idp",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "turbo": {
    "tags": [
      "app"
    ]
  },
  "scripts": {
    "dev": "nitro dev",
    "dev:app": "vite --config vite.config.ts",
    "build": "vite build --config vite.config.ts && nitro build",
    "build:app": "vite build --config vite.config.ts",
    "build:server": "nitro build",
    "preview": "node .output/server/index.mjs",
    "typecheck": "nitro prepare && tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@libsql/client": "^0.14.0",
    "@nuxt/ui": "^4.5.1",
    "@openape/auth": "workspace:*",
    "@openape/core": "workspace:*",
    "@openape/grants": "workspace:*",
    "@openape/vue-components": "workspace:*",
    "drizzle-orm": "^0.44.7",
    "h3": "catalog:",
    "jose": "catalog:",
    "nitropack": "^2.11.0",
    "vue": "catalog:",
    "vue-router": "^4.5.0"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "@vitejs/plugin-vue": "^5.2.0",
    "typescript": "catalog:",
    "vite": "^6.3.0",
    "vitest": "catalog:"
  },
  "engines": {
    "node": ">=22"
  }
}
