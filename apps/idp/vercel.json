{
  "buildCommand": "pnpm turbo run build --filter=@openape/auth --filter=@openape/core --filter=@openape/grants && npx vite build && NITRO_PRESET=vercel npx nitropack build && node scripts/vercel-post-build.mjs",
  "installCommand": "pnpm install --frozen-lockfile",
  "outputDirectory": ".vercel/output",
  "framework": "nitro"
}
