{
  "buildCommand": "pnpm turbo run build --filter=@openape/auth --filter=@openape/core --filter=@openape/grants && NITRO_PRESET=vercel npx nitropack build",
  "installCommand": "pnpm install --frozen-lockfile",
  "outputDirectory": ".vercel/output",
  "framework": "nitro"
}
