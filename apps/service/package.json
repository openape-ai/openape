{
  "name": "@openape/cloud",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "description": "OpenApe Cloud — Multi-Tenant Identity & Authorization SaaS",
  "author": "Delta Mind GmbH",
  "license": "MIT",
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev --port 3000",
    "typecheck": "nuxi prepare && nuxt typecheck",
    "test": "nuxi prepare && vitest --config vitest.config.ts",
    "test:run": "nuxi prepare && vitest run --config vitest.config.ts",
    "lint": "eslint ."
  },
  "dependencies": {
    "@nuxt/ui": "^4.4.0",
    "@openape/nuxt-auth-idp": "workspace:*",
    "@openape/unstorage-s3-driver": "workspace:*",
    "aws4fetch": "^1.0.20",
    "nuxt": "^4.0.0",
    "stripe": "^17.0.0",
    "tailwindcss": "^4.0.0"
  },
  "devDependencies": {
    "h3": "^1.15.5",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  },
  "engines": {
    "node": ">=22"
  }
}
