// AUTO-GENERATED from docs/flows.md by scripts/build-docs.mjs — do not edit.
export const docsHtml = "<h1>OpenApe Chat — flows</h1>\n<p>How a user gets into Chat. Screenshots are captured live from the containerized\nE2E stack (<code>compose/demo/run.sh</code>) and refreshed on every run.</p>\n<h2>One-click SSO</h2>\n<p>Chat is a DDISA <strong>Service Provider</strong>. A user who already has an IdP session (e.g.\nfrom signing into Troop) lands in Chat with <strong>one click</strong> — same passkey, same\nIdP, no second prompt. A first-time user is sent through the IdP&#39;s passkey +\nconsent once, then dropped straight back here.</p>\n<p>Chat&#39;s start page:</p>\n<p><img src=\"/docs/screenshots/08-chat-landing.png\" alt=\"Chat landing\"></p>\n<h2>Chat home</h2>\n<p>Signed into Chat as <code>demo@openape.test</code> via SSO — the conversation home, ready\nto talk to people and agents:</p>\n<p><img src=\"/docs/screenshots/09-chat-dashboard.png\" alt=\"Chat dashboard\"></p>\n"
