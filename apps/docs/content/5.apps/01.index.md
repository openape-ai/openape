---
title: Overview
description: "Every OpenApe app, documented from a live end-to-end run: OpenApe ID, Troop, Chat, Tasks, Plans, Testrun, Timetrack, PR, CRM."
---

# Apps

Every app below is documented from a live end-to-end run on the local stack — a headless browser drives the real flow, and the screenshots are captured as it goes. The guide cannot drift from the product because it *is* the test run.

::card-group
  ::card{title="OpenApe ID" icon="i-lucide-app-window" to="/apps/idp"}
  Register a WebAuthn passkey — no password, ever. Your OpenApe ID becomes the DDISA authority for your identity: every Service…
  ::
  ::card{title="Troop" icon="i-lucide-app-window" to="/apps/troop"}
  Troop is a DDISA Service Provider: it discovers your IdP from your email domain via a DNS TXT record and redirects you there to…
  ::
  ::card{title="Chat" icon="i-lucide-app-window" to="/apps/chat"}
  Chat is a DDISA Service Provider. With an existing IdP session (say, from Troop) you are in with a single click — same passkey…
  ::
  ::card{title="Tasks" icon="i-lucide-app-window" to="/apps/tasks"}
  Tasks keeps a shared to-do board for humans and agents. Sign in with your passkey, make a team, and drop work into its lanes —…
  ::
  ::card{title="Plans" icon="i-lucide-app-window" to="/apps/plans"}
  Plans keeps living, versioned plans a team edits together — the same document an agent reads and updates over the `ape-plans`…
  ::
  ::card{title="Testrun" icon="i-lucide-app-window" to="/apps/testrun"}
  Testrun turns a CI report into a public proof link. An agent (or `ape-testruns upload report.json`) pushes the run; you get a…
  ::
  ::card{title="Timetrack" icon="i-lucide-app-window" to="/apps/timetrack"}
  Timetrack logs hours against projects for you and your agents. Sign in, create a company and a project, and your monthly…
  ::
  ::card{title="PR" icon="i-lucide-app-window" to="/apps/pr"}
  When an agent opens a PR it lands here for a human to approve before anything merges. Sign in once and every agent diff waits…
  ::
  ::card{title="CRM" icon="i-lucide-app-window" to="/apps/crm"}
  CRM keeps deals, contacts and notes in one board — for you and for the agents working alongside you. Sign in, create a…
  ::
::
